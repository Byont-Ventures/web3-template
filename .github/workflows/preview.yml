name: Preview

on:
  push:
    branches-ignore:
      - main

jobs:
  format-test-lint:
    uses: Byont-Ventures/web3-template/.github/workflows/format-test-lint.yml@main
    secrets: inherit

  deploy:
    uses: Byont-Ventures/web3-template/.github/workflows/vercel.yml@main
    with:
      VERCEL_ENVIRONMENT: preview
    secrets: inherit
